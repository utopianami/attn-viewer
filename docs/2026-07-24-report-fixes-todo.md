# 시황 리포트 수정 목록 (2026-07-24 리뷰 세션)

**상태: 전 항목 일괄 처리 완료 (2026-07-24 오후).** 엔진 753·노드 75 테스트 그린,
codex 리뷰 1라운드(HIGH 6·MED 4·LOW 1) 중 8건 즉시 수정. 후속(스코프 초과)은 맨 아래.

리포트 `2026-07-24-5` 리뷰(사용자 + `2026-07-24-report-5-review-handoff.md`)에서 나온
수정 항목의 누적 기록. **한 번에 일괄 처리 예정** — 항목이 더 나오면 여기에 추가.

## A. 발행 안전성 — 의미론 게이트 (리뷰 인수인계 핵심)

실행 순서 중요: **A1을 먼저** 구현해 주장이 검증을 통과할 길을 연 다음 A2 게이트를
넣는다. 순서를 뒤집으면 모든 회차가 검증 0건이라 영구 hold가 된다.

- [x] **A1. 추가 조사 후 재검증 루프** — research findings를 반영해 주장 재검증
      (`report_pipeline.py`: 현재 verify/verify2가 research보다 앞이라 되먹임 없음.
      실증: q4가 c1의 CAPEX 둔화 전제를 반박했는데 c1이 그대로 실림)
- [x] **A2. 검증 0건 → 발행 상태 `hold`** — hold에서는 제목·결론이 원인 단정 금지
- [x] **A3. 제목 덮어쓰기 수정** — 조립기의 안전한 "(미검증)" 제목을
      `headline_from_article` H1이 덮어쓰지 않게. 제목은 검증된 주장 범위 안에서만
      단정 (예: 미검증이면 "…원인은 아직 분해할 수 없다" 형)
- [x] **A4. 합성 후 의미론 감사** — 제목·요약·결론이 검증 주장/근거로 뒷받침되는지
      검사 (`audit_article`은 현재 숫자 존재·단위만 봄). 시점·대상·비교기준(분모)
      불일치 데이터를 인과 근거로 쓰지 않았는지 포함
- [x] **A5. 글 내부 스탠스 일관성** — 제목·중반부 단정과 결말(겸손 절) 유보가
      모순되지 않게. 제목은 결말의 스탠스를 따른다

## B. 글 품질 — 템플릿 누출·가독성 (FRAME, `report_article.py`)

- [x] **B1. 템플릿 단계명 소제목 노출 금지** — "겸손 — …", "정리 + 관찰 신호 — …"
      같은 사고의-틀 내부 단계 이름을 소제목에 그대로 쓰지 말 것. 소제목은 내용
      문장형만 (2026-07-24 사용자: "겸손이 뭐야?")
- [x] **B2. TL;DR 불릿 박스** — 글 최상단 3~4줄 불릿 요약 고정 (벤치마크 골격 소폭
      변경 — 사용자 결정 대기 중이었으나 가독성 불만 반복으로 포함 권장)
- [x] ~~B3. 내부 용어 첫 언급 정의 규칙~~ (완료 2026-07-24: 국면N·티커·회사 약칭,
      FRAME 문체 규칙 추가. **확장 완료**: 주장 카드에서 재발해(사용자 2차 지적)
      합성(synth)·수정(revise) 프롬프트에도 동일 규칙 추가 — 카드 제목·개요·mechanism·
      counter의 국면2는 '가격 주도 구간(국면2)'로 소급 수정)
- [x] ~~B4. 근거 라벨 짧게 + 문장 끝 배치 규칙~~ (완료 2026-07-24: 상세는 입력값 표로)
- [x] **B5. 투자 권유 면책 문구 제거** — FRAME 문체 규칙의 "마지막 줄 고정: 본 글은
      투자 권유가 아닙니다…" 조항 삭제 + 기존 리포트(-5, 23-3) 본문에서도 제거
      (2026-07-24 사용자: "당연히 투자는 본인이 알아서" — 의미 없음)
- [x] **B6. 가독성·내용 추가 개선** — 사용자 리뷰 진행 중, 항목 추가 예정
      ("글이 읽기 너무 어렵고 내용도 이상함" — 구체 지점 수집 중)

## C. UI (`public/report.js`)

- [x] **C1. 상태 배너** — 본문 위에 한 줄 고정: "⚠ 주장 N건 모두 미검증 · 최종 의견:
      관망" (리뷰는 종합 전체를 본문 위로 올리라 했으나 첫 화면 가독성 훼손 —
      배너 절충안)
- [x] **C2. 주장 카드에 `status`(unverified 등) 노출** — 현재 "최종 주장"으로만 표시
- [x] **C3. 추가조사 배지 상세화** — 성공 건수만이 아니라 출처 포함/실패 건수 표기
- [x] ~~C4. 근거 라벨 시각적 축소(.src-ref)~~ (완료 2026-07-24)
- [x] ~~C5. 구간 raw ISO → "07-24 02:36" 형식~~ (완료 2026-07-24)
- [x] ~~C6. 표 가로 스크롤~~ (완료 2026-07-24)

## D. 계약·테스트

- [x] **D1. `openapi.yaml` 선갱신** — 발행 상태(`hold` 등)·`article_meta` 스키마 추가
      후 프론트·계약 테스트(`test/contract/market-report.contract.test.mjs`)가 따르게
- [x] **D2. 회귀 테스트** (리뷰 기준 승계)
      - 전 주장 unverified fixture에서 인과 확정 제목 발행 금지
      - 조사가 전제를 반박하면 해당 주장 재검증/제외
      - 기간·지표·대상이 다른 숫자는 근거로 불통과
      - UI에서 unverified/hold 표시 (모바일·데스크톱)
      - MoM/YoY 등 비교 기준(분모) 병기 강제

## E. 파이프라인 안정성 (이번 세션 발견, 코드 수정 완료분 포함)

- [x] **E1. anthropic API 폴백 400 전멸 수정** — Role의 anthropic 레그가 전 호출
      400 Bad Request (regen4 로그 실측). CLI 실패 시 안전망 부재. 원인 추정:
      `_options`의 thinking/output_config 파라미터 — 재현·수정 필요
- [x] **E2. cli_complete 파싱 재시도 예산 정리** — range(2) 재시도가 호출자 타임아웃
      밖에서 시간 2배 소모. research는 wait_for로 감쌌으나(완료) 다른 호출부도 점검
- [x] ~~E3. draft·compose 스테이지 2400s 확대~~ (완료 2026-07-24)
- [x] ~~E4. research 질문별 예산 강제(wait_for) + 빈 에러 문자열 방지~~ (완료)
- [x] ~~E5. 생성 시각 09:00/21:00 KST 변경~~ (완료, `.env`)

## F. 기능 확장 (사용자 요청, 계획 별도)

- [x] **F1. 거시 시황 분석 레이어** — 유가·금리·증시·환율 관측이 리포트 논증에
      들어가게. "어제 나스닥 전체가 폭락했는데 12시간마다 요약하는 곳에서 그 내용이
      없는 게 말이 안 된다"(2026-07-24 사용자). 구성 3단:
      1) **수집** — 거시 지표 수집기 추가: 나스닥·S&P(전일 등락), 미 10y 금리,
         달러인덱스, 원/달러, **엔 관련 지표(엔/달러, 엔 캐리 신호 — 사용자 명시)**,
         WTI 유가. 기존 yahoo_metrics 경로 재사용 검토(^IXIC, ^GSPC, ^TNX, DX-Y.NYB,
         KRW=X, JPY=X, CL=F). 단 메모리 가격 축 심화는 범위 외(yvon 소관)
      2) **합성 주입** — 합성(synth)·드래프트 단계에서 거시 이벤트·지표를 명시적으로
         포인트아웃: 거시 관측이 섹터 논증(수요·밸류에이션·위험선호)에 미치는 전이
         경로를 다루게 프롬프트·계약 확장
      3) **중요도 게이트** — 항상 넣는 게 아니라 중요할 때 넣는다(사용자):
         등락 폭·이벤트 중요도 임계 기준으로 포함 여부 판단 (예: 주요 지수 ±2%,
         유가 ±5%, 환율 급변 등 — 임계값은 구현 시 확정)
- [x] **F2. workflow-review.html 현행화** — 위 파이프라인 변경 일괄 반영 시 그래프·
      카드·불변식 갱신 + 스크린샷 확인 (메모리 규칙: 배포 전 필수)
- [x] **F3. "무슨 일이 있었나" 팩트 섹션 기본 탑재** — 창(12h)의 핵심 사실(가격 큰 폭
      하락 등)은 결과론이어도 **기본값으로 먼저** 깔리고, 심층 분석은 그 위에 얹는 구조
      (2026-07-24 사용자: "지난 시황 제일 중요한 건 가격이 큰 폭으로 떨어졌다인데 그
      내용이 기본으로 있고 그 다음 분석들이 추가로 있는 거지"). B2(TL;DR)·C1(상태
      배너)·F1(거시)과 묶어 리포트 상단 골격으로 설계
- [x] **F4. 반사이익(2차 수혜) 섹터 매핑** — 메모리·클라우드 호재 발생 시 인접 수혜
      섹터(전력·인프라 등) 정리를 리포트가 수행 (2026-07-24 사용자: "이틀 전 클라우드
      호재 때 전력 같은 반사이익 섹터를 정리해줘야 하는데 그런 탐색이 아예 안 됨").
      합성/드래프트에 '2차 전이 경로' 항목 추가 + 필요시 조사 질문으로 커버

## 참고

- 미커밋 변경 3파일: `report_article.py`(용어 정의·라벨 규칙·research 예산),
  `report_pipeline.py`(스테이지 타임아웃), `public/report.js`(가독성) — 이번 세션 것.
  일괄 수정 시 덮어쓰지 말고 이어서 작업.
- 오늘 밤 21:00 KST 자동 회차는 현재 코드(B3·B4·E3·E4 반영)로 돈다.

## 후속 (codex 리뷰에서 확인, 이번 스코프 밖 — 다음 작업 후보)

- [ ] **H3. verify3에 조사 근거 전달** — 재검증기가 클러스터·앵커만 보고 조사 결과를
      못 봄. 수정기가 반박 조사를 무시해도 못 잡는다 → report_verify에 findings 채널
- [ ] **H5. 의미론 감사 위반 시 본문 재작성 루프** — 현재는 제목 폴백+UI 경고까지만.
      위반 시 compose 1회 재시도(감사 problems 주입)로 본문 자체를 고치는 확장
- [ ] **L1. publish_status 응답 필수화** — 과거 리포트에 필드가 없어 optional 유지 중.
      과거분 마이그레이션 후 required 전환 + 계약 테스트 강화
